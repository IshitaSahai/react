//we create index file in the folders generally so that we can take the whole import here and then we can use all the imports from here to wherever required 

export {todocontext, Todoprovider, usetodo} from "./todocontext"

//usetodo contains all the methods and everything that's needed to be used, if we don't export it then we would've had to import everything separately 